FROM node:20-alpine
RUN apk add --no-cache netcat-openbsd
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
EXPOSE 3000
CMD ["sh", "-c", "until nc -z db 5432; do echo 'waiting for db...'; sleep 1; done && npx drizzle-kit push && npm run dev"]
